// src/js/utils/constants.js

export const CHAR_COUNT_WARNING_THRESHOLD = 15000; // Characters
export const API_REQUEST_TIMEOUT = 300000; // 5 minutes in milliseconds, example for future use

// Default settings if not found in localStorage or UI
export const DEFAULT_MODEL = 'gemini-1.5-flash-latest';
export const DEFAULT_TEMPERATURE = 0.7;
export const DEFAULT_TONE = 'Neutral';
export const DEFAULT_TARGET_LANG = 'en';

// Add other constants as needed
